# Upstash integration normally injects the Redis URL/token into Vercel automatically.
# These names are for manual local configuration only.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Optional; defaults to the Upstash token when unset.
VOTE_HASH_SECRET=
